import { combineReducers } from 'redux'
// import { uploaderReducers } from 'react-redux-uploader'


export default combineReducers({
  // uploaders: uploaderReducers, // This key name, 'uploaders', is important. Don't change it.
})
